// TODO: Implement the create user tool
// This tool should make a POST request to the backend to create a new user
// Look at get-user.js and delete-user.js for examples of how to structure the code
// The backend expects: { name: string, email: string }
// The backend URL is passed as the first parameter (backendUrl)
// The arguments are passed as the second parameter (args) with { name, email }

// eslint-disable-next-line no-unused-vars
export async function createUser(backendUrl, args) {
  // TODO: Implement this function
  // 1. Extract name and email from args
  // 2. Make a POST request to ${backendUrl}/users with the user data
  // 3. Return the response in the same format as get-user.js
  // 4. Handle errors appropriately
  
  throw new Error('create_user tool is not implemented yet')
}

